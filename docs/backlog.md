# Backlog

<!-- DRIFT GUARD — Do not remove the audience line below. -->
> **Audience: AI coding agents only.** AI-maintained backlog of deferred work. Append new entries at the end of `## Open` as `### <title>` followed by four bullets in this order — **Source:** (the plan, review or incident it came from), **Problem:**, **Why deferred:**, **Action on pickup:** (what the implementer needs to know). Remove an entry only when the work is done or dropped, moving anything durable to its owner first. Completed work belongs in `changelog.md`, standing lessons and decision records in `lessons.md`, concrete feature gaps in `future-todos.md`; this file holds only open work deferred from plans and reviews.

## Open

### Execution-guard form contradicts AGENTS.md §Important Implementation Notes #1

- **Source:** Plan Review of the self-update restart fix, 2026-09-06 (branch `worktree-self-update-restart`; the plan file is local to the author's machine).
- **Problem:** §Important Implementation Notes #1 mandates `[[ "${BASH_SOURCE[0]}" == "${0}" ]] && main "$@"`. The three `_download-*-scripts.sh` use that form, and so does §Complete Download Script Template, but `system-setup/system-setup.sh`, `kubernetes/kubernetes-setup.sh`, and AGENTS.md's other three snippets (the standalone-script example, §Orchestrator Pattern, and the module template) use `if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then main "$@"; fi`. The mandate therefore contradicts most of the repository, and AGENTS.md contradicts itself. A third form is also undocumented: the two test suites (`tests/test-self-update.sh`, `utils/tests/test-rsync-over-tunnel.sh`) have no execution guard at all, which is correct for development-only files that nothing sources and that are never distributed — but no rule records that exemption.
- **Why deferred:** No self-update behaviour is involved; this is a convention decision, not a defect, and the plan that surfaced it was scoped to the restart ordering.
- **Action on pickup:** Pick one canonical form and make AGENTS.md and every script agree. Weigh this first: the `&&` form is a false list when the file is sourced, so it returns 1 and the file's own `set -e` exits the sourcing shell — every test that sources such a script must write `source "$f" || true` (see `tests/README.md`). The `if` form returns 0 when sourced, so amending the note is the cheaper direction; standardising on `&&` instead means auditing every `source`-based test. Whichever form wins, state the test-file exemption in the same edit: a development-only test script carries no guard because nothing ever sources it.

### macOS system scope leaves other users' dotfiles owned by the invoking admin

- **Source:** Review of the shell history-control change, 2026-09-11.
- **Problem:** On macOS, `system-configuration.sh system` runs as a non-root admin and elevates per command. For another user's `~/.zshrc`, `create_config_file` creates the file through `run_elevated touch` (root-owned), and the replace path of `update_config_line` moves a `mktemp` file owned by the admin over it. The ownership restore at the end of `configure_shell_for_user` and `configure_shell_prompt_colors_user` is gated on `$EUID -eq 0`, which is never true there, so the target user ends up unable to write their own dotfile. Linux is unaffected (system scope runs as root, so the gate fires), and the `sed -i` path is expected to be unaffected too (GNU and BSD `sed -i` both `fchown` the rewritten file to the original owner, which succeeds as root; confirm on pickup).
- **Why deferred:** Pre-existing and macOS-only; not reproducible on the Linux host the 2026-09-11 change was reviewed on.
- **Action on pickup:** Replace the `$EUID -eq 0` gate in both functions with "the file is not the caller's own", restore ownership through `run_elevated chown`, and have `update_config_line` restore the original owner after its `mv` the way `backup_file` already does for backups. Verify on a macOS host with a second user account.
